<Window x:Class="YonetimFinansalIslemTakipSistemi.UI.Views.Users.UserFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        SizeToContent="Height"
        Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="pack://application:,,,/Assets/AppIcon.ico">

    <StackPanel Margin="20">

        <!-- Ad Soyad -->
        <Label Content="Ad Soyad:" Margin="0,0,0,2"/>
        <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>

        <!-- Kullanıcı Adı (edit modunda salt okunur) -->
        <Label Content="Kullanıcı Adı:" Margin="0,0,0,2"/>
        <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                 IsReadOnly="{Binding IsEditMode}"
                 Margin="0,0,0,10"/>

        <!-- Şifre -->
        <Label Content="{Binding PasswordLabel}" Margin="0,0,0,2"/>
        <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10"/>

        <!-- Aktif durumu -->
        <CheckBox Content="Aktif"
                  IsChecked="{Binding IsActive, Mode=TwoWay}"
                  Margin="0,0,0,14"/>

        <!-- Hata mesajı -->
        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Butonlar -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Kaydet"
                    Command="{Binding SaveCommand}"
                    IsDefault="True"
                    Width="80" Margin="0,0,8,0"/>
            <Button Content="İptal"
                    IsCancel="True"
                    Width="80"
                    Click="CancelButton_Click"/>
        </StackPanel>

    </StackPanel>
</Window>
