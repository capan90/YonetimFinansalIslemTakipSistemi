<Window x:Class="YonetimFinansalIslemTakipSistemi.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Giriş Yap"
        Height="300" Width="380"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Assets/AppIcon.ico">

    <StackPanel Margin="32,24">

        <TextBlock Text="Yönetim Sistemi"
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <Label Content="Kullanıcı Adı:" Padding="0,0,0,2"/>
        <TextBox x:Name="UserNameBox"
                 Text="{Binding UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Padding="4" Margin="0,0,0,10"/>

        <Label Content="Şifre:" Padding="0,0,0,2"/>
        <PasswordBox x:Name="PasswordBox"
                     Padding="4" Margin="0,0,0,8"/>

        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Button Content="Giriş Yap"
                Command="{Binding LoginCommand}"
                IsDefault="True"
                HorizontalAlignment="Center"
                Width="120" Padding="8,4"/>

    </StackPanel>
</Window>
